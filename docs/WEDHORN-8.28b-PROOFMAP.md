# Wedhorn 8.28(b) — top-down proof map (source of truth)

Built by reading `/private/tmp/wedhorn.txt` from the headline theorem downward. **This file is
the plan.** Per the CLAUDE.md *Wedhorn-faithfulness* rule, every Lean declaration on this chain
must mirror the node below and cite it (statement no. + page + `wedhorn.txt:LINE`). If a Lean
leaf has no node here, it is an artifact — delete it, don't grow it.

Citations are `wedhorn.txt:LINE`. "Lean:" pointers are **unverified hints** — re-read the code
before trusting them (never rely on memory for status).

---

## Headline

**Theorem 8.28(b)** — p. 80, wedhorn.txt:4051.
> "Let `A = (A, A+)` be an affinoid ring and `X = Spa A`. Assume … (b) `A` is a strongly
> noetherian Tate ring. Then `OX` is a sheaf of complete topological rings. Moreover
> `H^q(U, OX) = 0` for all `q ≥ 1` and all rational subsets `U`."

Proof of (b) — wedhorn.txt:4214:
> "By Proposition A.4 it suffices to show that every open covering by rational subsets is
> `OX`-acyclic. … Every open covering of `X` has a refinement `U = (Ut)` of the form
> `Ut := R(T/t)` with `T ⊆ A` generating `A` as an ideal (Lemma 7.54). … Thus by Proposition
> A.3 (2) it suffices to show the following lemma [8.34]."

So 8.28(b) = **A.4** (acyclic-on-basis ⇒ sheaf + `H^q=0`) ∘ reduction-to-rational-covers (Lemma 7.54)
∘ **A.3(2)** (refinement) ∘ **Lemma 8.34**.

---

## Dependency tree

```
Thm 8.28(b)  [4051]
├─ Prop A.4  [5332]   basis acyclic ⇒ sheaf + H^q=0
├─ Lemma 7.54         every cover refines to a rational cover gen by T, T·A=A   (VERIFY in repo)
├─ Prop A.3(2) [5315] refinement ⇒ same acyclicity
└─ Lemma 8.34 [4222]  rational cover gen by finite T (T·A=A) is OX-acyclic
   ├─ Lemma 8.33 [4151]  2-elt Laurent cover {U1,U2} acyclic  (diagram chase)
   │  ├─ Cor 8.32 [4142]   ε injective  (product restriction faithfully flat)
   │  │  ├─ Prop 8.30 [4095]  restriction OX(V)→OX(U) flat
   │  │  │  ├─ Example 6.38 [2693]  OX(V) is strongly noetherian Tate
   │  │  │  ├─ Remark 7.55          reduce to U1=R(f/1)/U2=R(1/f) forms        (VERIFY)
   │  │  │  └─ Lemma 8.31 [4106]  AhXi ff-flat over A; AhXi/(f−X),/(1−fX) flat over A
   │  │  │     └─ Remark 8.29 [4074]  M⊗_A AhXi ≅ MhXi  (5-lemma; needs Prop 6.18)
   │  │  └─ prime-surjectivity (flat + surj on Spec ⇒ faithfully flat)          (VERIFY)
   │  ├─ Example 6.38 [2693] + Example 6.39 [2708]  explicit OX(U1),OX(U2),OX(U1∩U2)
   │  └─ explicit diagram chase  [4169-4210]  (λ, λ′, ι; columns exact by 8.2.1)
   ├─ Cor 7.32 [—]      unit s separating the cover                            (VERIFY)
   └─ Prop A.3 (1)(3) [5315]  product / mutual-refinement of acyclic covers
```

`Example 6.38`'s own subtree (the part the recent session got wrong):

```
Example 6.38 [2693]  "Â⟨T/s⟩ is again strongly noetherian"
└─ ι : A → Â⟨T/s⟩ is topologically of finite type            [2695]
   └─ Remark 6.37(1) [2682]  t.f.t. over strongly-noeth ⇒ strongly-noeth
      ├─ Prop+Def 6.36 [2675]  strongly noeth ⟺ (i) Â⟨X₁..Xₙ⟩ noeth ∀n ⟺ (ii) every t.f.t. Tate ring noeth
      └─ Prop 6.33 [2645]      t.f.t. closed under composition
         └─ Prop 6.34 [2654]   over a Tate ring, t.f.t. ⟺ strictly t.f.t.  (+ §6.6 Prop 6.29/6.31)
   (separately, the C/a representation [2700]: Â⟨T/s⟩ = C/a, C=Â⟨X..⟩ noeth by 6.36(i),
    a closed by Prop 6.17 — this gives NOETHERIAN, not the strong part)
```

---

## Nodes (with verbatim quotes)

### Prop A.4 — p. 105, wedhorn.txt:5332
Basis `B` stable under finite ∩; `F` defined by `F(V) = lim_{U⊆V,U∈B} F'(U)`. **If `F` is
`U`-acyclic for every `U∈B` and every `B`-covering, then `F` is a sheaf and
`Ȟ^q(U,F)→H^q(U,F)` iso; in particular `H^q(U,F)=0` for `U∈B`, `q≥1`.**
- The bridge from "all rational covers acyclic" to "`OX` is a sheaf + `H^q=0`".
- Lean: `Presheaf`/`StructureSheaf` sheaf-from-acyclic glue (VERIFY) + the project's Prop A.3/A.4 chain.

### Prop A.3 — p. 105, wedhorn.txt:5315
(1) mutual refinement ⇒ same acyclicity; (2) `V` refines `U` ⇒ same acyclicity; (3) `U×V`
acyclic ⟺ `U` acyclic. Hypothesis throughout: `V|U_{i0..iq}` acyclic for all tuples.
- Lean: the project's `propA3_*` chain (memory says complete + Wedhorn-faithful via intersection
  cover — VERIFY).

### Lemma 8.34 — p. 83, wedhorn.txt:4222
> "Let `A` be a complete strongly noetherian Tate ring and `U` a rational cover generated by
> finite `T ⊆ A` with `T·A = A`. Then `U` is `OX`-acyclic."
Proof structure (4225-4255): (i) `Uf={R(f/1),R(1/f)}` acyclic by **8.33**; restrictions stay
acyclic; Laurent covers `Uf1×…×Ufr` acyclic by **A.3(3)** induction. (ii) any `T`-cover refines
to one generated by units (Cor 7.32). (iii) unit-generated covers refine to Laurent covers.
(iv) assemble via **A.3(1)**.

### Lemma 8.33 — p. 82, wedhorn.txt:4151
2-element cover `{U1,U2}`, `U1={x(f)≤1}`, `U2={x(f)≥1}`: augmented Čech complex
`0→OX(X)→OX(U1)×OX(U2)→OX(U1∩U2)→0` is exact.
- ε injective by **Cor 8.32** (4161). Explicit rings by **6.38/6.39** (8.2.1, 4163-4167):
  `OX(U1)=A⟨ζ⟩/(f−ζ)`, `OX(U2)=A⟨η⟩/(1−fη)`, `OX(U1∩U2)=A⟨ζ,ζ⁻¹⟩/(f−ζ)`.
- Diagram chase (4169-4210): rows via `λ(g,h)=g(ζ)−h(ζ⁻¹)`, `λ′`; columns exact by 8.2.1;
  surjectivity of `λ,λ′` from `A⟨ζ,ζ⁻¹⟩=A⟨ζ⟩+ζ⁻¹A⟨ζ⁻¹⟩`; `im ι = ker λ`.

### Cor 8.32 — p. 82, wedhorn.txt:4142
`(Ui)` finite rational cover ⇒ `OX(X)→∏OX(Ui)` is **faithfully flat (in particular injective)**.
- From **Prop 8.30** (each restriction flat) + prime-surjectivity. NO separate proof in Wedhorn
  (immediate). Lean: `Cor832.lean` (VERIFY — composed this session).

### Prop 8.30 — p. 81, wedhorn.txt:4095
`U⊆V⊆X` rational ⇒ `OX(V)→OX(U)` flat.
> "By Example 6.38 we know that `OX(V)` is again a strongly noetherian Tate ring. Thus we may
> assume `X=V`, `A` complete. By Remark 7.55 … `U=U1=R(f/1)` or `U2=R(1/f)`. … `OX(U1)=Â⟨X⟩/(f−X)`
> … Thus it suffices to show [Lemma 8.31]."

### Lemma 8.31 — p. 82, wedhorn.txt:4106
`A` noetherian complete Tate: (1) `AhXi` faithfully flat over `A`; (2) `AhXi/(f−X)`,
`AhXi/(1−fX)` flat over `A`.
- (1) injectivity-preservation via **Remark 8.29** + `q={∑aνXν : a0∈p}` prime over `p`.
- (2) `wg` injective for `g=1−fX` (easy) and `g=f−X` (the `M′` f.g.-noetherian argument, 4131-4138).

### Remark 8.29 — p. 81, wedhorn.txt:4074
`A` complete noetherian Tate, `M` f.g. with canonical topology (**Prop 6.18(1)**): the map
`µM : M⊗_A AhXi → MhXi`, `m⊗a ↦ ma`, is **bijective**.
> "clear if `M` is f.g. free. In general … presentation `Aⁿ→Aᵐ→M→0` (A noetherian). Prop 6.18(2)
> shows `u,p` continuous and open onto image. … 5-lemma implies the claim."

### Example 6.38 — p. 54, wedhorn.txt:2693  ★ (the node the recent session diverged from)
`ι : A → Â⟨T/s⟩` is **topologically of finite type** (4695). If `A` is strongly noetherian, then
`Â⟨T/s⟩` is **again strongly noetherian** (2707).
> "If `A` is a strongly noetherian Tate ring, we may represent `Â⟨T/s⟩` as a quotient of a ring
> of restricted power series. Set `C = Â⟨X_{i,t}⟩` … `a` the ideal gen by `{t − si X_{i,t}}`. By
> hypothesis `C` is noetherian and hence `a` is a closed ideal (Prop 6.17). … In particular
> `Â⟨T/s⟩` is again strongly noetherian."
- **The strong-noetherian conclusion is via t.f.t. + Remark 6.37(1)**, NOT via the `C/a` quotient.
  The `C/a` paragraph gives *noetherian* (`C` noeth by 6.36(i); `a` closed by 6.17). The "strongly"
  is the t.f.t. argument. **No restricted-PS Fubini, no Banach-OMT openness anywhere.**
- Lean: `presheafValue_isStronglyNoetherian_faithful` (TopologyComparison) — currently `sorry`,
  awaiting the t.f.t. route. The 6.37(3) variant `presheafValue_isStronglyNoetherian`
  (TateAcyclicityResiduals) needs `[IsNoetherianRing D₀.P.A₀]`, which 8.28(b) does not supply.

### Remark 6.37(1) — p. 54, wedhorn.txt:2682
> "If `A` is a strongly noetherian Tate ring, then every Tate ring topologically of finite type
> over `A` is strongly noetherian by Proposition 6.33."

### Prop+Def 6.36 — p. 53, wedhorn.txt:2675
`A` strongly noetherian ⟺ (i) `Â⟨X₁..Xₙ⟩` noetherian ∀ `n∈ℕ₀` ⟺ (ii) every Tate ring t.f.t.
over `A` is noetherian. (Equivalence "follows immediately from 6.34".)
- Lean: `IsStronglyNoetherian` (RestrictedPowerSeries) is condition (i). The (i)⟺(ii) bridge and
  6.37(1) are the **missing infrastructure**.

### Prop 6.33 — p. 53, wedhorn.txt:2645
t.f.t. is closed under composition; and `ψ∘ϕ` t.f.t. ⇒ `ψ` t.f.t. (via Prop 6.29(iv), 6.31(iii)).

### Prop 6.34 — p. 53, wedhorn.txt:2654
Over a Tate ring `A`, `ϕ:A→B` (B complete f-adic) is t.f.t. ⟺ strictly t.f.t.; then `B` Tate, `ϕ` adic.

### Prop 6.17 — p. 50, wedhorn.txt:2449  ⚠ PROOF MISSING IN SOURCE
`A` complete Tate, `M` complete with countable nbhd basis of 0: `M` noetherian ⟺ every submodule
closed. In particular `A` noetherian ⟺ every ideal closed.
- **Wedhorn writes "Proof. Missing" (2454)** — this is an external input (the project's closed-ideal
  keystone). Lean: `Wedhorn.isClosed_ideal_of_ringNoetherian` (NoetherianTateModules — VERIFY,
  carries the Banach-OMT/6.18 gap).

### Prop 6.18 — p. 50, wedhorn.txt:2456  ⚠ PROOF MISSING IN SOURCE
`A` complete noetherian Tate: (1) f.g. modules have a unique complete module topology w/ countable
nbhd basis; (2) linear maps of f.g. modules are continuous + open onto image.
- **"Proof. Missing" (2463).** Feeds Remark 8.29. External input.

---

## The gaps, ranked (what to formalize, in Wedhorn-faithful order)

1. **t.f.t. infrastructure (§6.6): Prop 6.29 / 6.31 / 6.33** + **Def 6.36 (i)⟺(ii)** + **Remark 6.37(1)**.
   This is the clean replacement for the scrapped Fubini/quotient route. It discharges
   `presheafValue_isStronglyNoetherian_faithful` once `presheafValue D` (= `Â⟨T/s⟩`) is shown
   t.f.t. over `A` (Example 6.38, 2695). **NOT yet in the repo.**
2. **Prop 6.17 / 6.18** — stated without proof in this source; treat as keystone external inputs
   (likely [BGR]/Banach-OMT). Confirm the repo's keystone matches the *statement*.
3. The 8.33/8.34 Čech assembly + 8.31/8.30/8.32 flatness — substantially present in the repo
   (`Cor832`, `propA3_*`, Laurent-cover files); audit each against the nodes above.

---

## Method (binding, from CLAUDE.md)
- No code on this chain without the node + a verbatim quote.
- "I must build substantial missing infrastructure" ⇒ re-read the node; you've likely left the route.
- Re-read `wedhorn.txt` and the `.lean`, never memory, for every statement and status.

---

## Task 2 plan — Wedhorn §6.6 topologically-of-finite-type infrastructure (the case-(b) unblock)

This is the faithful replacement for the scrapped Fubini/quotient route, and the unblock for the
broken baseline (LaurentRefinementCore needs `IsStronglyNoetherian (presheafValue D₀)`). It is a
**substantial, mostly-from-scratch formalization** — essentially Wedhorn §6.6 — none of it in the repo.

Leaves, in dependency order (each cites `wedhorn.txt:LINE`):
- **Def 6.28** (wedhorn.txt:2569) — `ϕ : A → B` *strictly t.f.t.*: ∃ `n` + surjective continuous open
  `A`-algebra hom `Â⟨X₁..Xₙ⟩ ↠ B`. (A `Prop`/`structure`.)
- **Prop+Def 6.29** (wedhorn.txt:2576) — `ϕ` *t.f.t.*: four equivalent conditions (i)-(iv). For our
  use only **(i)** (surjective cont. open `Â⟨X⟩_T ↠ B`) and **(ii)** (the `A[M]` dense / rings-of-def
  form) are needed. ⚠ The full (i)⟺(ii)⟺(iii)⟺(iv) equivalence is hard and (iii)⇒(iv) **cites external
  [Hu1] 2.3.25** (wedhorn.txt:2608) — treat as an external input or prove only the implications we use.
- **Prop 6.31** (wedhorn.txt:2626) — strict variant (likely not needed for the headline; defer).
- **Example 6.32(2)** (wedhorn.txt:2637) — **`A → Â⟨T/s⟩` is t.f.t.** (via 6.29(ii), `N={t/sᵢ}`, `M=N∪{1/s}`).
  This is the concrete fact about `presheafValue D`. KEY LEAF.
- **Prop 6.33(1)** (wedhorn.txt:2645) — t.f.t. closed under composition (via 6.29(iv)+6.31(iii)).
- **Prop+Def 6.36 (i)⟺(ii)** (wedhorn.txt:2675) — strongly noeth ⟺ every t.f.t. Tate ring over `A`
  is noetherian. ("follows immediately from 6.34", wedhorn.txt:2654.)
- **Remark 6.37(1)** (wedhorn.txt:2682) — `A` strongly noeth ⇒ every t.f.t. Tate ring over `A` is
  *strongly* noeth (compose 6.33 + 6.36(ii)).

Assembly: `presheafValue D = Â⟨T/s⟩` is t.f.t. over `A` (6.32(2)) ⇒ (6.37(1)) strongly noetherian.
Discharges `presheafValue_isStronglyNoetherian_faithful` (TopologyComparison) faithfully.

Then, to fix the broken baseline: make the result **instance-resolvable** (so
`IsStronglyNoetherian (presheafValue D₀)` synthesizes — the `hb`/`hT_pb`/`hA_complete` inputs must come
from typeclasses available at the LaurentRefinementCore call sites) and update those call sites to the
migrated `presheafValueCanonicalQuotientEquiv` signature. Only then does `lake build` go green again.

SCOPE NOTE: this is genuinely a large piece (the §6.6 theory + the 6.36/6.37 bridge + instance plumbing).
It should be staged (definitions → Example 6.32(2) → 6.33 → 6.36/6.37 → assembly → instance → caller fix),
each stage `lake build`-verified (note: stages live UPSTREAM of the LaurentRefinementCore red, so they ARE
verifiable; the baseline only goes green at the final caller-fix stage).

---

## Task-2 progress note (2026-05-31) — first prerequisite surfaced

Started the §6.6 build per the rule; the very first leaf (Def 6.28, the t.f.t. predicate) revealed a
**foundational prerequisite the repo lacks: the topology on the multi-variable Tate algebra
`Â⟨X₁,…,Xₙ⟩ = restrictedMvPowerSeriesSubring n A`.** Only the single-variable `TateAlgebra A` is
topologised (`instTopologicalSpaceTateAlgebra` = `tateAlgBasis'.topology`, needs `[IsTateRing A]`);
`restrictedMvPowerSeriesSubring n A` has **no `TopologicalSpace` instance**, so the *continuous open*
hom in Def 6.28 cannot even be stated. (`IsStronglyNoetherian` only needs the subring's `CommRing`,
not its topology — hence the gap.) Do NOT parametrise the topology as a hypothesis (that's the
forbidden deferral); build it.

**§6.6 leaf order (revised):**
0. **`restrictedMvPowerSeriesSubring n A` topology** — the n-variable analogue of `tateAlgBasis'`
   (a `RingSubgroupsBasis`: "all coefficients in `Uₘ` for all but finitely many monomials"), making
   it a Tate ring. PREREQUISITE for everything below.
1. `IsStrictlyTft (ϕ : R →+* S)` — Wedhorn **Def 6.28** (wedhorn.txt:2569): `∃ n` and a surjective
   continuous open `R`-algebra hom `↥(restrictedMvPowerSeriesSubring n R) ↠ S`. (Statement drafted;
   needs leaf 0 to typecheck `Continuous`/`IsOpenMap`.)
2. `isStronglyNoetherian_of_isStrictlyTft` — Wedhorn **Remark 6.37(1)** (wedhorn.txt:2682) via Prop
   6.33 + Def 6.36: t.f.t. over a strongly-noetherian Tate ring ⇒ strongly noetherian. (sorry)
3. `presheafValue_isStrictlyTft` — Wedhorn **Example 6.32(2)/6.38** (wedhorn.txt:2637): `A → Â⟨T/s⟩`
   is (strictly, by Prop 6.34) t.f.t. (sorry)
4. `presheafValue_isStronglyNoetherian` (faithful) = compose 2 ∘ 3.

(The drafted `IsStrictlyTft` def + the `isStronglyNoetherian_of_isStrictlyTft` bridge were written and
reverted only because leaf 0 is missing — they compile the moment the n-variable topology exists.)
