# Decomposition — `example638_evalHom_range_isClosed` via the FAITHFUL Example 6.38 route

**Date:** 2026-06-03 (CORRECTED after the B2 reversal) · **Mode:** `/develop` Phase 1e.
Supersedes the withdrawn ring-of-def "route correction" (b2_log 2026-06-03).

## Goal (R)
```lean
-- Wedhorn828.lean:1214 — the lone base-change sorry
private theorem example638_evalHom_range_isClosed
    [IsTateRing A] [IsNoetherianRing A] [IsStronglyNoetherian A] (D : RationalLocData A) :
    IsClosed (Set.range (example638_evalHom D))
-- (ambient section also gives [CompleteSpace A] (right-uniform, Wedhorn828.lean:53), [T2Space A])
```
Discharging R makes `presheafValue_isNoetherianRing_residual` (1292) sorry-free → Cor 8.32 → IsSheafy.

## Plain-English proof (transcribed from Example 6.38, wedhorn.txt:2700–2707)
> "Set **C = Â⟨Xᵢ,ₜ⟩** and let **a** be the ideal of C generated by **{t − sᵢXᵢ,ₜ}**. By hypothesis,
> **C is noetherian and hence a is a closed ideal (Proposition 6.17)**. As A is a Tate ring,
> Tᵢ·A = A (Rem 7.30(2)), so the image of sᵢ in C/a is a unit. Now A → Â⟨T/s⟩ and A → C/a satisfy
> the same universal property."

`C = restrictedMvPowerSeriesSubring n A` (`n = D.T.card`) is a **complete Tate ring** (Prop 6.21(2)).
`C` is **noetherian** (`IsStronglyNoetherian` (i)). `a := ker(example638_evalHom D)` is an ideal of `C`;
by **Prop 6.17** `a` is **closed**. So `C/a` is complete, and the induced map `C/a → presheafValue D`
is the universal-property isomorphism `C/a ≅ Â⟨T/s⟩` — whence `example638_evalHom` is **surjective**
and its range is all of `presheafValue D`, in particular **closed**.

**The general-`n` Tate topology on `C` IS faithful — it is Prop 6.21(2).** (My earlier `/develop`
pass wrongly called it a trap; see the b2_log entry. The topology-on-`C` route is literally
Example 6.38's proof.)

## n=1/n=2 templates to mirror (NOT iterate — direct, avoids Fubini)
- `TateAlgebra A = restrictedMvPowerSeriesSubring 1 A`; topology in `TateAlgebraTopology.lean`:
  `pairSubring P = A₀⟨X⟩` (:193, ring of def), `pairIdeal P = I·A₀⟨X⟩` (:247, ideal of def),
  `tateAlgNhd P n` (:336), `RingSubgroupsBasis`→`tateAlgebraTopology'` (:902), instances (:937–983),
  `tateAlgebraTopology'_completeSpace` (:1064, needs `[IsTateRing A][T2Space A][CompleteSpace A]`).
- `TateAlgebra₂` (n=2): DIRECT, identical architecture via `tateAlgNhd₂` (:2246–2615). Confirms the
  `Fin n` generalization is "same proof, n instead of 1/2".

## Lemmas

- **L1** (API GAP — the substantial new infra): general-`n` Tate **topology** on
  `C = restrictedMvPowerSeriesSubring n A`. Source: **Prop 6.21(2)** (wedhorn.txt:2487, verbatim:
  *"A⟨X⟩_T is an f-adic ring, B⟨X⟩ is a ring of definition, I⟨X⟩ = I·B⟨X⟩ is a finitely generated
  ideal of definition. If A is a Tate ring, then A⟨X⟩_T is a Tate ring."*). Mirror the n=1 stack to
  `Fin n` in a NEW file `MvTateAlgebraTopology.lean` (do not touch working n=1/n=2). Sub-pieces:
  - **L1a** `mvPairSubring n P = A₀⟨X₁..Xₙ⟩ = restrictedMvPowerSeriesSubring n P.A₀` (ring of def);
    `mvPairIdeal n P = I·mvPairSubring` (ideal of def). [mirror :193/:247]
  - **L1b** `mvTateAlgNhd` + `RingSubgroupsBasis` ⟹ `TopologicalSpace`, `IsTopologicalRing`,
    `UniformSpace`, `IsUniformAddGroup`, `NonarchimedeanRing`, `IsTateRing`, `T2Space`,
    `(uniformity C).IsCountablyGenerated` on `C`. [mirror :336/:902/:937–983]
  - Attacks: edge `n=0` → `C=Â`, topology = A's, holds; `n=1` must defeq/agree with `TateAlgebra`
    (or be a separate instance — acceptable). Source-drift: Prop 6.21(2) is exactly "A⟨X⟩_T Tate".
    SURVIVED. Sizing: n=1 file ~1000 LOC; expect comparable for `Fin n` (the source proof is
    Prop 6.21(2), ~6 lines, but the formalisation mirrors a known ~1000-LOC template).

- **L2** (API GAP): `CompleteSpace C`. Source: Â⟨X⟩ = completion (Prop 6.21(2) "ring of restricted
  power series"). Mirror `tateAlgebraTopology'_completeSpace` (:1064) — coefficient-wise Cauchy for
  `Fin n →₀ ℕ`. Attacks: `n=0` → `CompleteSpace A` (have it); needs `[CompleteSpace A][T2Space A]`.
  SURVIVED. Sizing: comparable to the n=1 completeness proof.

- **L3** (leaf, project, DONE): `IsNoetherianRing C` = `IsStronglyNoetherian.isNoetherianRing_restricted
  (D.T.card)` (RestrictedPowerSeries.lean:241). No new work. Faithful (whole-ring noeth, NOT noeth-A₀).

- **L4** (leaf, project — INSTANTIATION of landed §3.7.2/1): faithful **Prop 6.17** for `C`:
  every ideal `J` of `C` is closed. Discharge: `fg_topologicalClosure_isClosed`
  (WedhornBanachTheorem.lean:505) at `M = A = C` (self-module); `J.topologicalClosure` is f.g.
  because `C` is noetherian (L3) ⟹ `IsClosed (J : Set C)`. **NO noeth-A₀** (the repo's
  `isClosed_ideal_of_noetherian`, NoetherianTateModules.lean:458, uses the noeth-A₀ Krull route —
  load-bearing Step 3 `isClosed_ideal_of_adicComplete_noetherian` on `P.A₀` — which FAILS for ℂ_p
  since `O⟨X⟩` is not noetherian; do NOT use it). Source: Prop 6.17 (wedhorn.txt:2449, "A noetherian
  ⟺ every ideal closed"). Needs L1 (C's UniformSpace/IsUniformAddGroup/IsTopologicalRing/T2Space/
  IsTateRing/uniformity-cg) + L2 (CompleteSpace C) + L3 (C noeth) + `ContinuousSMul C C` (from
  IsTopologicalRing). Attacks: discharge — verified `fg_topologicalClosure_isClosed` exists at
  :505 with exactly these instances; `J.topologicalClosure` f.g. from `IsNoetherian` (submodule of
  noeth is f.g.). SURVIVED.

- **L5** (internal): discharge R. `a := ker(example638_evalHom D)` ideal of `C` ⟹ closed (L4);
  `C/a` complete (C complete + a closed); the induced `C/a → presheafValue D` is the
  universal-property iso `C/a ≅ Â⟨T/s⟩` (Wedhorn: "same universal property", Cor 5.50) ⟹
  `example638_evalHom` surjective ⟹ `range = univ` ⟹ closed. Sub-pieces:
  - **L5a** `example638_evalHom` is **continuous** w.r.t. C's L1 topology (eval at power-bounded
    tuple; mirror n=1 `evalHomBounded` continuity). Needed for the quotient/iso.
  - **L5b** `a` closed (L4) ⟹ `C/a` complete + Hausdorff (mathlib: complete quotient by closed
    subgroup; `tateAlgebra`-quotient completeness templates in Wedhorn828.lean:443/483/520).
  - **L5c** `C/a ≅ presheafValue D` (universal property) — OR the lighter `range` closed via
    `C/a` complete + `example638_evalHom_denseRange` (already proven) + `ē` uniform-inducing.
    Pick whichever composes; the iso is Wedhorn's literal claim and gives surjectivity directly.
  - Attacks: composition — `a` is genuinely an ideal (`RingHom.ker`); `C/a ≅ presheafValue` is the
    completed-rational-localization universal property (Cor 5.50); `n=0`/whole-space cross-checks
    against the DONE `presheafValueCanonicalQuotientEquiv_faithful`. SURVIVED (L5c is the subtlest;
    may sub-ticket during execution).

## NOT a trap (correcting the prior pass)
The general-`n` Tate topology on `C` (L1/L2) IS Prop 6.21(2) — Wedhorn's actual Example 6.38 route.
The ring-of-def route (A·B₀+B₀⊆range) was the invented one and is ill-posed (relations ⟹ no
coefficients). The `/develop` case study's rejected route was Fubini+OMT; this is neither (direct
`Fin n` construction, no Fubini; Prop 6.17 via the landed §3.7.2/1, no fresh OMT).

## Feasibility
Every leaf discharged or sourced: L1/L2 = Prop 6.21(2) mirror of a known ~1000-LOC n=1 template
(substantial but well-templated, direct not iterated → no Fubini); L3 DONE; L4 = instantiate landed
`fg_topologicalClosure_isClosed` (no noeth-A₀); L5 = ker-closed + quotient-complete + universal-
property iso (Cor 5.50). Largest risk: L5c (the iso) — sub-tickets during execution if needed.
