# Expert-review session state (round 7)

- Generated: 2026-05-15 (same day as round 6; follow-up to round 6's reply)
- Audience: ChatGPT Pro (continuing series, round 7 follow-up)
- Goal of brief: Verify that the revised W2/W3/W4 statements correctly applied round-6 reviewer's feedback. Compact follow-up; reviewer already saw round-6 context.
- Scope: revised W2 (restricted-cover-by-units), W3 (relative labels) + W3-transport, W4 (prune theorem Option C), I.1 chain composition.
- Reply received: false
- Reply integrated: false

## Questions in the brief

| # | Question (verbatim from §4 of the brief) |
|---|------------------------------------------|
| Q1 | Does the revised W2 statement (with the new `restricted_standard_cover_generated_by_units` clause) now faithfully capture Wedhorn 8.34 Step (ii)? Specifically, is the cover-decomposing clause the right formal expression of "the restricted cover U|L is generated by units"? Or is "generated by" stronger? |
| Q2 | Is the W3 + W3-transport split correctly structured? W3 produces a relative tree with `allSplitsInducing` only; W3-transport descends to absolute tree with both `allSplitsInducing` AND `Refines L C`. Right division of labor? |
| Q3 | Is W4's prune statement provable? (a) Is "any tree with `Refines + allSplitsInducing` has a pruned equivalent with `Refines + allSplitsInducing + allNodesDisjoint` and same `toCovering`" true in this generality? (b) If yes, is the natural proof structural induction? |
| Q4 | Does the I.1 chain (W1 → W2 → W3 → W3-transport → graft → W4 prune) correctly compose? Any subtle dependencies (typeclass coherence on presheafValue L, hidden hypotheses from Group III) the chain might miss? |

## Round-6 changes applied

- W1: kept absolute span-top, added audit note.
- W2: dropped allNodesDisjoint; strengthened unit property to `restricted_standard_cover_generated_by_units`; baked in Cor 7.32 implicitly.
- W3: rewritten with relative labels — returns `LaurentTree (presheafValue L)` with only `allSplitsInducing`.
- W3-transport (NEW): lifts relative to absolute with `allSplitsInducing` AND `Refines L C`.
- W4: replaced cross-leaf disjointness (which was false) with prune theorem (Option C).

## File state at brief time

- 7 sorries: W1, W2, W3, W3-transport, W4, I.1's body, V.1.
- `lake build` clean.
- I.4 (allNodesDisjoint_graftAt_prune) and W5 (graftAt_allNodesDisjoint) still closed axiom-clean (legacy, no longer on I.1's main path).
