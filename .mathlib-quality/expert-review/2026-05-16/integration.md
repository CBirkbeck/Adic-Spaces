# Reply integration — round 20, 2026-05-16

Reply received from ChatGPT Pro on 2026-05-16. Brief: `brief.md`. Reply: `reply.md`.

## Interpretation summary

Reviewer's core message: **the right construction for P3 adds finitely many generators of `P.I^N` to the numerator set `T_plus`**. This makes `hopen` trivial and the valuation inequalities are automatic on the target subset.

The key new sub-lemma is the **domination lemma**: for a denominator `a` nonvanishing on a quasi-compact rational set `K`, there exist `N` and a finite generating set `B_N` of `P.I^N` such that `v(b) ≤ v(a)` on all of `K` for `b ∈ B_N`.

| # | Reviewer point | Maps to | Type |
|---|---|---|---|
| 1 | Add B_N generators of P.I^N to T_plus; hopen trivialises | Q1 (P3) | direct answer, new construction |
| 2 | Domination lemma is the real sub-lemma | Q1 (P3) | new sub-lemma to spawn |
| 3 | T_plus = {t*h | t∈L.T} ∪ {L.s*g} ∪ B_N, s_plus = L.s*h, P_plus = L.P | Q1 (P3) | explicit construction |
| 4 | rationalOpen equality: extra B_N inequalities are automatic on K⁺ | Q1 (P3) | proof technique |
| 5 | hopen proof: b∈I^N is A₀-linear combo of B_N elements → b/s_plus is linear combo of generators | Q1 (P3) | proof technique |
| 6 | Q2 (7.49 reverse) is mathematically sound via strict-triangle Cauchy, but NOT on P3 critical path | Q2 (P4) | de-prioritises |
| 7 | Filter-level key lemma for 7.49 reverse if needed: "∀ᶠ x y in F.prod F, v(x-y) < γ ⇒ v x = v y" | Q2 (P4) | proof technique |
| 8 | Risks: don't search for 1/h, B_N must be in T from start, K⁺/K⁻ compactness, coefficient boundedness if P.A₀ ⊄ A⁺ | meta | warnings |
| 9 | Q3–Q9 not addressed (P5, P6, P7, T-WEDHORN-213-DATUM, T001, Stacks 00MA, meta) | Q3-Q9 | UNANSWERED |

## Action plan

1. **Spawn sub-ticket**: `exists_ideal_pow_generators_dominated_on_compact` — the domination lemma. This is P3's real prerequisite.
2. **Close P3** (`exists_absolute_ratio_rationalLocData_aux` + `relative_ratio_split_transports_to_RatioNodeData`) using the domination lemma + explicit B_N construction. No A-side inverse hypotheses.
3. **Defer P4 leaf case**: per reviewer, not on P3 critical path. Will revisit after P5/P6/P7.
4. **Re-ask Q3–Q9** in a follow-up round if needed; for now P3 unblocks W3-transport (P4) once P5 (W3) produces the relative tree.

## Implementation status (2026-05-16, post-reply)

P3 structure follows the reviewer's plan exactly:
- `exists_ideal_pow_generators_dominated_on_K` (domination lemma): stated, body `sorry`. Substantive content (quasi-compactness + I-adic decay).
- `exists_absolute_ratio_rationalLocData_aux` (P3 sub-lemma): construction packaging in place.
  - **`hopen_plus`, `hopen_minus`** closed via `Submodule.span_induction` over the B_N generators.
  - **Both `rationalOpen` equalities** closed via `ValuativeRel.mul_vle_mul_iff_left` cancellations + bridge non-vanishing + domination lemma's `hBplus_dom`/`hBminus_dom`.
  - 2 remaining inner sorries: `IsCompact Kplus`, `IsCompact Kminus`.
- `relative_ratio_split_transports_to_RatioNodeData` (P3): unchanged from before — packages P3 sub-lemma's output into `RatioNodeData L g h`.

The compactness sub-sorries need a "standard Tate pair" extraction theorem: from `IsTateRing A`, produce a `PairOfDefinition` `P_T` with principal ideal `P_T.I = Ideal.span {π}` generated by a pseudouniformizer `π ∈ P_T.A₀`. The project has `IsTateRing.pseudoUniformizer` (giving `w : Aˣ` topologically nilpotent) but no extraction to a principal pair. Adding the witness `(π, hI, hπ_tn, hπ_unit, hArch)` to P3's signature as parameters is the convenience-cheat the user disallowed; the right move is to write the standard-Tate-pair extraction lemma (or to ask the reviewer for the cleanest route).

## Net session result

- **Reviewer's plan implemented honestly.** No A-side inverse hypothesis added.
- **6 internal sorries reduced to 3** inside the P3 sub-lemma (compactness × 2 + the domination lemma, the latter as a separate declaration).
- The domination lemma is now the **single substantive mathematical sorry** for P3 (the compactness sub-sorries reduce to a standard-Tate-pair extraction, which is project infrastructure, not deep math).

Full project builds (3127 jobs).

## Changes applied this round

- Reply saved at `.mathlib-quality/expert-review/2026-05-16/reply.md`.
- State updated: Reply received: true, Reply integrated: true.
- Next: implement the domination lemma + close P3 unconditionally.

## Open questions still on the table

- Q3 (P5): explicit recursion for ratio Laurent tree from I_units — not addressed; may need its own round.
- Q4 (P6): dominating unit construction (Cor 7.32) + per-leaf I_units — not addressed.
- Q5 (P7): Zavyalov §2.3 + Wedhorn 7.14 standard cover construction — not addressed.
- Q6 (T-WEDHORN-213-DATUM forward): algebraic identity transporting hopen through ρ_E — not addressed.
- Q7 (T001): Bourbaki CA III §2.8 argument — not addressed.
- Q8 (Stacks 00MA): mathlib gap status — not addressed.
- Q9 (meta): downstream substantive theorems — not addressed.
